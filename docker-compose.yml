version: '3.8'
services:
  mongodb:
    image: mongo
    container_name: tomadb
    ports:
      - "30000:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: tomafinaldb

volumes:
  mongo-data: